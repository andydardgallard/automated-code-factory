diff --git a/src/pricing.py b/src/pricing.py
--- a/src/pricing.py
+++ b/src/pricing.py
@@ -1,5 +1,6 @@
-def calc_total(items):
-    total = 0.0
-    for item in items:
-        total += item.price
-    return total
+def total_price(items):
+    """Sum the price of every line item."""
+    total = 0.0
+    for item in items:
+        total += item.price
+    return total
diff --git a/tests/test_pricing.py b/tests/test_pricing.py
--- a/tests/test_pricing.py
+++ b/tests/test_pricing.py
@@ -1,5 +1,12 @@
-from src.pricing import calc_total
+import types
+
+from src.pricing import total_price
 
 
 def test_empty_cart():
-    assert calc_total([]) == 0.0
+    assert total_price([]) == 0.0
+
+
+def test_single_item():
+    item = types.SimpleNamespace(price=2.5)
+    assert total_price([item]) == 2.5
